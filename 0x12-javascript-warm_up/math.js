// Create a function named "add" that takes two integer arguments and returns their sum
function add(a, b) {
    return a + b;
  }
  
  // Make the "add" function visible from outside by exporting it
  module.exports = add;
  